<?xml version="1.0" encoding="UTF-8"?>
<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>

<VBox xmlns="http://javafx.com/javafx"
      xmlns:fx="http://javafx.com/fxml"
      fx:controller="com.mstrust.client.teacher.controller.SubjectManagementController"
      spacing="10" styleClass="content-container">
    
    <padding>
        <Insets top="20" right="20" bottom="20" left="20"/>
    </padding>
    
    <!-- Header -->
    <HBox alignment="CENTER_LEFT" spacing="15">
        <Label text="Quản lý Môn học" styleClass="page-title"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Button fx:id="createButton" text="+ Tạo Môn học Mới" styleClass="primary-button" 
                onAction="#handleCreateSubject"/>
        <Button fx:id="refreshButton" text="🔄 Làm mới" styleClass="secondary-button"
                onAction="#handleRefresh"/>
    </HBox>
    
    <!-- Filters Section -->
    <HBox spacing="10" alignment="CENTER_LEFT" styleClass="filter-bar">
        <Label text="Bộ lọc:"/>
        
        <!-- Department Filter -->
        <VBox spacing="3">
            <Label text="Khoa/Bộ môn" styleClass="filter-label"/>
            <ComboBox fx:id="departmentFilter" promptText="Tất cả khoa" prefWidth="200"/>
        </VBox>
        
        <!-- Search Field -->
        <VBox spacing="3" HBox.hgrow="ALWAYS">
            <Label text="Tìm kiếm" styleClass="filter-label"/>
            <TextField fx:id="searchField" promptText="Nhập mã môn hoặc tên môn học..." 
                      prefWidth="300"/>
        </VBox>
        
        <VBox spacing="3" alignment="BOTTOM_CENTER">
            <Label text=" "/>
            <Button text="🔍 Tìm" styleClass="primary-button" onAction="#handleSearch"/>
        </VBox>
        
        <VBox spacing="3" alignment="BOTTOM_CENTER">
            <Label text=" "/>
            <Button text="↻ Reset" styleClass="secondary-button" onAction="#handleResetFilters"/>
        </VBox>
    </HBox>
    
    <!-- Statistics Bar -->
    <HBox spacing="15" alignment="CENTER_LEFT" styleClass="stats-bar">
        <Label fx:id="totalSubjectsLabel" text="Tổng số: 0 môn học"/>
        <Separator orientation="VERTICAL"/>
        <Label fx:id="departmentCountLabel" text="Số khoa: 0"/>
    </HBox>
    
    <!-- Table View with Loading Overlay -->
    <StackPane VBox.vgrow="ALWAYS">
        <TableView fx:id="subjectsTable">
            <columns>
                <TableColumn fx:id="idColumn" text="ID" prefWidth="50" sortable="true"/>
                <TableColumn fx:id="codeColumn" text="Mã môn học" prefWidth="120" sortable="true"/>
                <TableColumn fx:id="nameColumn" text="Tên môn học" prefWidth="250" sortable="true"/>
                <TableColumn fx:id="creditsColumn" text="Số tín chỉ" prefWidth="100" sortable="true"/>
                <TableColumn fx:id="departmentColumn" text="Khoa/Bộ môn" prefWidth="180" sortable="true"/>
                <TableColumn fx:id="descriptionColumn" text="Mô tả" prefWidth="200" sortable="false"/>
                <TableColumn fx:id="actionsColumn" text="Thao tác" prefWidth="150" sortable="false"/>
            </columns>
            <placeholder>
                <Label text="Không có môn học nào.Nhấn 'Tạo Môn học Mới' để bắt đầu."/>
            </placeholder>
        </TableView>
        
        <!-- Loading Overlay -->
        <StackPane fx:id="loadingPane" visible="false" managed="false" 
                   style="-fx-background-color: rgba(0,0,0,0.3);">
            <ProgressIndicator style="-fx-progress-color: white; -fx-background-color: transparent; -fx-background-radius: 0; -fx-padding: 0;"/>
        </StackPane>
    </StackPane>
    
    <!-- Pagination Bar -->
    <HBox spacing="10" alignment="CENTER" styleClass="pagination-bar">
        <Button fx:id="firstPageButton" text="⏮ Đầu" onAction="#handleFirstPage"/>
        <Button fx:id="prevPageButton" text="◀ Trước" onAction="#handlePreviousPage"/>
        <Label fx:id="pageInfoLabel" text="Trang 1 / 1"/>
        <Button fx:id="nextPageButton" text="Sau ▶" onAction="#handleNextPage"/>
        <Button fx:id="lastPageButton" text="Cuối ⏭" onAction="#handleLastPage"/>
        <Region HBox.hgrow="ALWAYS"/>
        <Label text="Số môn học / trang:"/>
        <ComboBox fx:id="pageSizeComboBox" value="20" prefWidth="80"/>
    </HBox>
</VBox>
