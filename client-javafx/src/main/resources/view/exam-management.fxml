<?xml version="1.0" encoding="UTF-8"?>

<?import javafx.geometry.Insets?>
<?import javafx.scene.control.*?>
<?import javafx.scene.layout.*?>
<?import javafx.scene.text.Text?>

<!-- 
  Exam Management Screen - For Teacher/Admin
  Màn hình quản lý đề thi cho giáo viên và admin
  @author: K24DTCN210-NVMANH (30/11/2025)
-->

<StackPane xmlns="http://javafx.com/javafx"
           xmlns:fx="http://javafx.com/fxml"
           fx:controller="com.mstrust.client.teacher.controller.ExamManagementController"
           stylesheets="@../css/exam-common.css"
           style="-fx-background-color: white;"
           prefWidth="1000" prefHeight="700">
    
    <!-- Main Content -->
    <BorderPane>
        
        <!-- Header -->
        <top>
            <VBox styleClass="header-section">
                <padding>
                    <Insets top="20" right="20" bottom="20" left="20"/>
                </padding>
                
                <!-- Title Row -->
                <HBox alignment="CENTER_LEFT" spacing="10">
                    <Label text="📝 Quản Lý Đề Thi" styleClass="page-title"/>
                    <Region HBox.hgrow="ALWAYS"/>
                    <Button fx:id="createExamButton" 
                            text="✨ Tạo đề thi" 
                            onAction="#handleCreateExam"
                            styleClass="primary-button"
                            style="-fx-background-color: #2196F3; -fx-text-fill: white; -fx-font-weight: bold; -fx-padding: 8 15; -fx-background-radius: 5px; -fx-cursor: hand;"
                            visible="true"
                            managed="true"
                            minWidth="130"
                            prefWidth="130"/>
                </HBox>
                
                <!-- Filters Row -->
                <HBox spacing="15" alignment="CENTER_LEFT">
                    <padding>
                        <Insets top="15"/>
                    </padding>
                    
                    <!-- Subject Filter -->
                    <VBox spacing="5">
                        <Label text="Môn học:" styleClass="filter-label"/>
                        <ComboBox fx:id="subjectFilterCombo" 
                                  promptText="Tất cả môn học"
                                  prefWidth="200"/>
                    </VBox>
                    
                    <!-- Status Filter -->
                    <VBox spacing="5">
                        <Label text="Trạng thái:" styleClass="filter-label"/>
                        <ComboBox fx:id="statusFilterCombo" 
                                  promptText="Tất cả trạng thái"
                                  prefWidth="200"/>
                    </VBox>
                    
                    <!-- Published Filter (Teacher only) -->
                    <VBox spacing="5">
                        <Label text="Xuất bản:" styleClass="filter-label"/>
                        <ComboBox fx:id="publishedFilterCombo" 
                                  promptText="Tất cả"
                                  prefWidth="150"/>
                    </VBox>
                    
                    <!-- Spacer -->
                    <Region HBox.hgrow="ALWAYS"/>
                    
                    <!-- Refresh Button -->
                    <Button fx:id="refreshButton" 
                            text="🔄 Làm mới" 
                            onAction="#onRefresh"
                            styleClass="secondary-button"/>
                </HBox>
            </VBox>
        </top>
        
        <!-- Main Content - Exam List -->
        <center>
            <ScrollPane fitToWidth="true" fitToHeight="true">
                <padding>
                    <Insets top="10" right="20" bottom="10" left="20"/>
                </padding>
                
                <!-- Exam Cards Container -->
                <VBox fx:id="examCardsContainer" spacing="8">
                    <!-- Exam cards sẽ được add dynamically từ controller (compact design) -->
                    
                    <!-- Empty State (hiển thị khi không có đề thi) -->
                    <VBox fx:id="emptyStateBox" 
                          alignment="CENTER" 
                          spacing="20"
                          visible="false"
                          managed="false"
                          prefHeight="400">
                        <Text text="📭" style="-fx-font-size: 72px;"/>
                        <Label text="Chưa có đề thi nào" 
                               styleClass="empty-state-text"/>
                        <Label text="Nhấn 'Tạo đề thi' để tạo đề thi mới" 
                               styleClass="empty-state-subtext"/>
                    </VBox>
                </VBox>
            </ScrollPane>
        </center>
        
        <!-- Footer - Info -->
        <bottom>
            <HBox styleClass="footer-section" alignment="CENTER" spacing="10">
                <padding>
                    <Insets top="10" right="20" bottom="10" left="20"/>
                </padding>
                
                <Label fx:id="examCountLabel" text="Tìm thấy 0 đề thi"/>
                
                <Region HBox.hgrow="ALWAYS"/>
                
                <Label fx:id="lastRefreshLabel" text="Cập nhật lần cuối: --:--"/>
            </HBox>
        </bottom>
        
    </BorderPane>
    
    <!-- Loading Overlay -->
    <StackPane fx:id="loadingOverlay" 
               visible="false"
               style="-fx-background-color: rgba(0, 0, 0, 0.7);">
        <VBox alignment="CENTER" spacing="20">
            <ProgressIndicator prefWidth="80" prefHeight="80"/>
            <Label fx:id="loadingMessage" 
                   text="Đang tải dữ liệu..."
                   style="-fx-text-fill: white; -fx-font-size: 18px; -fx-font-weight: bold;"/>
        </VBox>
    </StackPane>
    
</StackPane>

